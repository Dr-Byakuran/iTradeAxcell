@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
    @Html.AntiForgeryToken()

    <ul class="nav navbar-nav navbar-right">

        @*<li class="dropdown">
            <a href="#" id="dropdownBtn" class="dropdown-toggle" data-toggle="dropdown">
                HQ <span class="caret"></span>
            </a>
            <ul class="dropdown-menu animated fadeInLeft">
                <li><a href="#">HQ</a></li>
                <li class="divider"></li>
                <li><a href="#">SR</a></li>
            </ul>
        </li>*@


        <li class="dropdown navbar-user">
            <a href="javascript:;" class="dropdown-toggle" data-toggle="dropdown">
                <img src="~/assets/img/person_s.png" alt="" />
                <span class="hidden-xs">@User.Identity.GetUserName() </span> <b class="caret"></b>
            </a>
            <ul class="dropdown-menu animated fadeInLeft">
                <li class="arrow"></li>
                <li><a href="/Manage/ChangePassword">Change Password</a></li>
                <li><a href="javascript:;">Setting</a></li>
                <li class="divider"></li>
                <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
            </ul>

        </li>
    </ul>

    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>
}


<script>
    var dropdown = document.getElementsByClassName('dropdown-toggle')[0],
                    options = document.getElementsByClassName('dropdown-menu')[0];

        options.addEventListener("click", function (e) {
        if (e.target.tagName === 'A') {
            dropdown.firstChild.textContent = e.target.textContent + ' ';
        }
    });
</script>